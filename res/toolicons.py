import pyglet

PATH   = "res/tools_icons/"
PENCIL = pyglet.resource.image(PATH+"pencil.png")
ERASER = pyglet.resource.image(PATH+"eraser.png")
BUCKET = pyglet.resource.image(PATH+"bucket.png")
COLORS = pyglet.resource.image(PATH+"colors.png")
